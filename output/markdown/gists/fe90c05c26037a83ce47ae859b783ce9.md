# [encrypt file using a keyring] #gpg #keyring

## encrypt file using a keyring.sh

```shell
gpg --homedir=mono/system/keyrings/default --encrypt -o SOME_FILE.gpg -r SOME_ONE@SOME_DOMAIN.com SOME_FILE.md
```


# Ruby DI Container Example (Dim)

## DI-Container.rb

```ruby
require "dim" # gem install dim

module Hai
  class Container < Dim::Container
    def initialize
      super
      setup
    end

    private

    def setup
      register(:foo) do |c|
        #
      end
    end
  end
end
```

